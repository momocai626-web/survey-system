請幫我生成一個完整的問卷系統，需求如下：

1. 前端問卷 (index.html)
- 使用純 HTML + CSS + JavaScript，不依賴外部套件。
- 題目包含：
  - GitHub 帳號註冊與使用（單選）
    - 完全不熟悉
    - 聽過但沒用過
    - 曾經註冊過帳號
    - 熟悉 Repo 建立與 Push
  - VS Code 安裝與操作（單選）
    - 沒安裝過 VS Code
    - 安裝過但不常用
    - 熟悉基本操作（開檔案、編輯程式）
    - 熟悉插件安裝與設定
  - Qwen Code 擴充元件（單選）
    - 沒聽過
    - 聽過但沒用過
    - 安裝過但不熟悉功能
    - 熟悉並常用
  - GitHub Pages 部署網站（單選）
    - 完全不熟悉
    - 知道有這功能但沒用過
    - 曾經部署過網站
    - 熟悉部署流程並能獨立完成
- 開放式問題：
  - 你希望在課程中學到什麼？
  - 你覺得目前寫程式最大的困難是什麼？
- 表單送出後，將結果透過 Google Apps Script (GAS) 寫入 Google Sheets。
- 送出使用 fetch POST + mode: no-cors，不需等待回應。

2. 後台管理頁面 (admin.html)
- 使用純 HTML + CSS + JavaScript。
- 從 Google Apps Script (GAS) GET 讀取 Google Sheets 資料（JSON 格式）。
- 自動統計各選項數量，並以長條圖視覺化顯示。
- 開放式問題以文字列表顯示。
- 提供「重新整理」按鈕。

3. Google Apps Script (GAS) 設定
- doPost(e)：解析 JSON body，寫入 Google Sheets，欄位順序為：
  GitHub, VSCode, Qwen, Pages, Learn, Difficulty, Timestamp
- doGet(e)：讀取 Sheets 所有資料，以第一列為 header（toLowerCase），
  回傳 JSON 陣列。
- 部署設定：執行身分「我」，存取權「所有人（含匿名）」。
- 目前使用的 GAS URL：
  https://script.google.com/macros/s/AKfycbx51RlYFqcX9zhN1COxSRqDi2extdKb8xIo6bZ8jugpRgBgx1tlb4Q7RJe7hM3R3eAO/exec

4. 部署到 GitHub Pages
- 請生成 README.md，說明如何：
  - 建立 GitHub Repo
  - Push index.html、admin.html
  - 啟用 GitHub Pages
  - 學員透過 GitHub Pages 填寫問卷
  - 講師透過 admin.html 查看統計結果

要求：
- 程式碼必須能直接在瀏覽器執行，不需要安裝任何套件。
- 資料存在 Google Sheets，支援跨裝置提交。
- 後台能即時讀取並統計。

--- 待新增 ---
- 課後問卷（另一個 index 頁面，題目待定）
